# 🔴 常设规则 · 美股编年史站点线

> **这一页是规则，不是历史。** 动手前读这一页；出处与来龙去脉在 [HANDOFF.md](HANDOFF.md) 对应小节。
> 每条后面的 `§N` 是它在 HANDOFF 里的出处，想知道"为什么"就去翻那一节。
>
> **为什么单独拆出来**（2026-08-07 Klay 拍板）：HANDOFF 是编年体、已 3300+ 行，
> 规则散在叙事节里，新会话靠 grep 找，找不到就复发同型错。
> 所以：**规则住这里（常设、去时间化）／HANDOFF 住编年（原样不改、可 grep）**。
>
> **本页只收「以后每次都适用」的约束。** 一次性的事实、当时的判断、已结案的待办，一律留在 HANDOFF，不往这里搬。
> 新增规则时：写清约束 + 标 `§N` 出处；**没有出处的不许进本页**。

---

## 一、身份与提交

| 规则 | 出处 |
|---|---|
| 提交署名一律 `Klay <klaywang24@gmail.com>`，**绝不加 Co-Authored-By**；判据 `git log -1 --format='%an <%ae>'` | 项目地图抬头 |
| **只 `git add` 自己动过的文件（禁 `-A`）**；CI 每天自 commit 数据，推前先 `git pull --rebase` | 硬规矩 §5 |
| **push 与 `gh workflow run` 由 Klay 在终端自跑**（分类器拦 AI push） | 硬规矩 §6 |

## 二、中文与 i18n

| 规则 | 出处 |
|---|---|
| **中文破折号「——」一律改冒号「：」，全域无例外**；英文侧单破折号「—」不动。提交前 `grep -c "——" index.html js/i18n.js` **必须为 0** | 硬规矩 §1 |
| **改中文正文必同步改 i18n 的 D 键源文本**（逐字一致，含全角标点），否则英文态整段泄漏 | 硬规矩 §2 · §34.4 |
| **改定价权益必须中英同改**：EN 定价面板由 `js/docs-i18n.js` 整块替换，只删中文＝英文访客照旧看见 | §37.4 |
| i18n **键一律不留首尾空格**，空格放拼接侧（带空格的键会静默失效） | §34.4 |
| 函数型 formatter 里的中文，`JSON.stringify` 扫不到，**必走 `translate()`**；验收必须**执行 formatter 看输出**（第四层扫描） | 硬规矩 §4 |
| 长段落文档页**不走短词条字典**，走 `js/docs-i18n.js` 的 `DOC_TR` | §9.2 |
| `SRC_OVERRIDES` 的源名不走 i18n，**源名一律纯英文** | §26.10 |

## 三、发布与缓存

| 规则 | 出处 |
|---|---|
| **改 JS/CSS 必 bump `index.html` 的 `?v=`**；自测刷三层缓存（sw + 浏览器 + `?v=`） | 硬规矩 §3 |
| `?v=` **只 bump `index.html` 里那四处**；**`data/*.json` 绝不加 `?v=`**（sw 对 /data/ 走网络优先）；**`sw.js` 的 `CACHE` 常量不动** | §16.4 |
| **改 CSS/JS 后必须先清三层缓存再量**（不清就量＝量到旧文件，已复发四次） | §21.4 · §30.6 · §31.9 · §45 |
| 第 4 层：**同版本号下改 JS，本地自测也吃 HTTP 缓存**，需 `fetch(url,{cache:'reload'})` | §26.10 |

## 四、路由与 Pages

| 规则 | 出处 |
|---|---|
| 🚨 **永远不要往仓库根加 `404.html`**：加了 Pages 会从「SPA 兜底」切走，**深链全挂** | §38.2 · §56 |
| `_redirects` **只写尾斜杠，故意不写 `/*`** | §38.2 |
| 🚨 **改 `index.html` 之后必须重跑 `python3 scripts/build_route_pages.py`**，否则 16 个路由页与首页漂移。脚本锚串校验报错是提醒你同步，不是让你绕过 | §49 |
| 🚨 **`ROUTES` 增删路由必须同步改 `.gitattributes`**，否则新页污染语言统计、旧页留悬空条目 | §56 |
| **创始价带码链接不能断**：`s` 以外的 query 一律保留 | §38.3 |

## 五、前端实现

| 规则 | 出处 |
|---|---|
| 图表**必须走 registry 契约**（`chart(...)` + `buildOne`），不自建 echarts 实例 | §45 |
| 图表兜底：`buildOne` try/catch，数据缺失显示占位，**绝不无声空白** | §三 |
| **下拉菜单必须 `position:fixed`**：`.tabs` 有 `overflow-x:auto` ⇒ overflow-y 被算成 auto，absolute 子元素被裁掉且量尺读不出来 | §37.5 |
| **站上有四套调色板**，写在文件末段的 `:root:not(.dark-mode)` 与 `.dark-mode` 精修**才是真正生效的** | §37.6 |
| **未知态一律不显示**，比先显示再撤安全 | §37.7 |
| 新块级元素**自己负责与后继元素的间距**（下边距 0 会紧贴） | §49.8 |

## 六、数据与台账

| 规则 | 出处 |
|---|---|
| 台账**只追加、绝不重写**；一行＝一次快照事件（**不是日历格**） | §33 |
| **已发布的结算期永不覆盖**；官方改了往 `revisions[]` 另记一行，旧值原样留着 | §31.4 · §28 |
| 三条源全空**抛异常留旧文件，绝不写空台账** | §35.4 |
| `TRACKED` 加新文件**只往后加，绝不改既有条目口径**；改口径＝另起新链 | §33.5 |
| **已写入的日期永不重取**（FINRA 不修订历史，这让本表天然是前向台账） | §30.2 |
| **`data/kindex_signals.json` 的文件名与数据 URL 绝不能改**（公开数据入口，Kaggle/HF/方法论页都回链） | §16.5 |
| 新增数据文件**必须在 `data/README.md` 登记** | 项目地图 §二 |

## 七、日期与口径

| 规则 | 出处 |
|---|---|
| **日期标签一律＝数据的美东交易日，`now()` 不许推日期**；`generated_at`/`updated` 这类精确时刻保持 UTC 不动（那是时刻不是日期） | §57 |
| **打包/发版前先 `anchor_hashes.py --verify`**：链断了还发版＝给坏证据盖官方图章 | §48.3 |
| **可验证性表述必须按时段拆分**，绝不能让「带 Git 时间戳、事后不可改写」盖住回填段 | §13.5 · §48.2 |
| 对外话术只说「**公开可查**」，**永不说「不可篡改」**（Wayback 站主可请求除名） | §26.3 |
| 🚨 **三道口径隔离**：本家族全是 30 天、旗舰是 1 年，**混比即错** | §30.1 |
| 「做空占比高 ≠ 有人看空」：FINRA 这份没按交易者类别拆，**「谁在押注恐惧」那句话这里说不得** | §30.2 |
| 置换检验用**固定种子 PRNG**：台账站的数字不能每次刷新都变 | §34.3 |

## 八、指标身份与命名

| 规则 | 出处 |
|---|---|
| **身份在 `series_id`，名字只是门牌**：显示名可换，序列身份不动 | §58 |
| **改公式口径不叫改名，必须起 v2 新序列**；永不可变的是「序列身份+公式版本+历史谱系」 | §49.15 |
| 家族命名用**并列兄弟名**（KAPX / KAPX-Aggressive / KAPX-Balanced），**绝不用 2.0/3.0** | §15 |
| 指数**设计已锁定，改＝另立新兄弟** | §16.1 |

## 九、合规与措辞红线

| 规则 | 出处 |
|---|---|
| 🔴 **绝不拿指数测收益/择时**；措辞永远「贵/便宜」，**绝不「买/卖/加仓」** | §16.1 |
| 🔴 **永不以择时/入场工具名义营销、永不测收益**（支付服务商 AUP 明文禁 "trading signals and strategies"，审核读整站含退款页） | §16.5 |
| 🔒 **任何批量替换必须加护身符排除名单**：机械正则会把「不是**策略**」改成语义崩坏的句子。受保护句见 §16.5 原文 | §16.5 |
| **兴登堡预兆永不做**（崩盘预测信号，撞两条红线）；**绝不发 `implied_10y_forecast`**，只存位置读数 | §35.8 · §36.3 |
| **回测段永远叫回测，永不并入前向计数** | §39.3 |
| **永不瞎编**：拿不到真实时在线数就不编（编假数＝踩最高铁律） | §41 |
| **付费内容/付费数据绝不入本公开仓库**；付费档案链接**只进邮件，绝不上站、绝不进 welcome 页、绝不进本仓库任何文件** | §七 · §9.6 · §18 |
| **接入收款＝站上每一处「我们卖不卖东西」的陈述同时变现在时**，四条必须一次改完 | §16.5 |
| 数字＝事实、美国法下不受版权；**别用改动数据当反抄袭指纹**（违反事实铁律与不可改写承诺） | §23.6 |

## 十、监控与闸

| 规则 | 出处 |
|---|---|
| 📌 **`if: always()` 只保证告警器会跑，不保证它说真话**：告警器必须知道 job 状态 | §17.2 |
| `JOB_STATUS != success` 时 **`sys.exit(0)` 绝不发正常播报** | §17.2 |
| **`unknown` 必须能升级**：两头不沾＝告警永不关闭＝蒙眼探测器 | §49.1 · §49.10 |
| Wayback 锚定步 `continue-on-error: true` + 逐条 `|| true` + `-m 60`：**它挂了不许影响管线与告警** | §26.3 |
| **生成失败绝不能把「量已抢到」报成整轮失败**（紧急的事被不紧急的事的失败掩盖） | §49.7 |
| 出口**永不 fail 构建**：站停更比出一处分歧更糟 | §47 |
| **盘点监控先查「已有的闸响过没有」，再谈加新闸** | §17.10 |
| 上游+后备双失败**明确抛错，绝不静默** | §49.10 |

## 十一、验收与量尺

| 规则 | 出处 |
|---|---|
| 📌 **验收失败时先怀疑量尺，再怀疑被测物**（一夜九次全是量尺） | §21.4 |
| **线上验证一律 `curl -s https://chronicle.klay-wang.com/`**，不能 curl `/index.html`（308 空响应） | §20.4 |
| 配图验收**必须 Read 打开 PNG 亲眼看，永不靠尺寸推测**；Chrome 必须 `--headless=new`（旧 flag 静默失败不产图） | §19.5 |
| 🚨 **自造 bug 只在真跑时现形，「写完看一遍」一个都抓不到** | §49.1 |
| 用户说「还是没解决」时，**第一步是让他圈出具体元素**，不是再量一遍自己修过的东西 | §22.1 |
| 视觉需求**别按字面翻译成 CSS**：先渲一张 mockup 给用户确认 | §22.3 |

## 十二、配图与文案

| 规则 | 出处 |
|---|---|
| 🎨 **配图铁律：一张图如果需要读者先懂术语才能看懂，它就不是图** | §19.5 |
| 同理移植到文案：**一句话若需先懂术语才能看懂，它就不是解释，是复述** | §22.13 |
| **图 100% 复用永不做第二版，文案必须分渠道** | §19.4 |
| **模板只定长度，不定内容**；判读那句**永远现写**（两期雷同＝$29 的理由当场消失） | §19.2 |
| **分类由读数定，不由日历定**：日历决定你几点起床，读数决定你写多长 | §19.2 · §22.4 |
| 头条大数字配色 ≥60 猩红 `#FF2400` / ≤40 绿 `#14A63E` / 中间金 `#B8893E`，**永不砖红** | §17.9 |
| **权益表里永不写会变的清单**（硬编码清单必腐烂，已复发五次） | §19.1 |
| 图卡**自解释五件套**（结论式标题/怎么读/当日注释/历史坐标/护身符+三点位水印） | §25 |

## 十三、发送与分发

| 规则 | 出处 |
|---|---|
| 🚨 **digest 发送对象绝不能再选 Everyone**；免费名单只收极端读数信 | §39.2 |
| **每日判读正文站上永不公开**（自第 2 期起是铁律） | §18 · §39.1 |
| **每日 digest 永不进 `digest_archive.json`**（该表只收周报/月报） | §46 |
| `digest_archive` 的**标题与链接取自 Buttondown 存档页实况，绝不手编** | §45 |
| **文案那两三句与点发送永不自动化**（AI 味源头 + 人工闸） | §17.9 |
| **永不手动把订阅者改成 Regular**（双重确认＝营销同意记录） | §23.2 |
| **评论区绝不提价格与订阅** | §23.3 |
| 每平台**主帖只发 1 条**，其余全是回复区弹药 | §23.3 |
| X thread：**图必须放第 1 条**；cashtag 用大写 `$SPY`，**不用 `#` 话题标签** | §23.1 |

## 十四、文档维护

| 规则 | 出处 |
|---|---|
| ⚠️ **改 HANDOFF 正文必须同时改抬头「最后更新／最新章节」**（已过期过多次，抬头是读者判断"还算不算数"的唯一依据） | HANDOFF 抬头 |
| HANDOFF **按时间追加，与前文冲突处一律以编号最大的那节为准** | HANDOFF 抬头 |
| 🔑 **双写原则**：硬规矩既进纠错文档，也进**会被自动加载的地方**（记忆／本页／skill） | §36.8 |
| **改完按线回填**：手册 · 记忆 · 纠错记录三样同步，查表见 `README-项目地图.md` §二 | 项目地图 |
| **一处判据只能有一个实现**；「全类」的边界＝该判据出现的所有地方 | 记忆 `market-chronicle` |

---

## 📌 本页怎么维护

1. **新规则进本页，新故事进 HANDOFF。** 一件事同时产生两者时，两边都写，本页那条标 `§N` 指回去。
2. **每条必须有出处。** 没有 `§N` 的规则不许进，否则下一手无从判断它是真规矩还是某次的临时决定。
3. **本页只增不改口径**：某条被推翻时，不要静默删掉，改成「已推翻，见 §N」——推翻史本身是资产（见记忆 `promise-mechanism-errata-doctrine`）。
4. 本页 2026-08-07 首建，规则从 HANDOFF §一～§58 全文提炼，逐条回指出处。
